{
	"plugin": "QuickCheck",
	"filename": "quickcheck.json",
	"schema": {
		"properties": {
			"timeout": {
				"type": "integer",
				"title": "The timeout in milliseconds",
				"default": 5000
			}
		}
	}
}